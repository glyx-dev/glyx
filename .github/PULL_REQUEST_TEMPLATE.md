## What changed

A short summary of the change and which crates/packages it touches.

## Why

The problem this solves, with a link to the issue or discussion if there is
one. Closes #

## How it was verified

- [ ] `cargo check --workspace` — zero warnings
- [ ] `bun test` (for JS changes)
- [ ] Manually verified in an example app (say which, and on which renderer backend)
- [ ] Renderer changes checked on all three backends (TinySkia / FemtoVG / Vello), or N/A

## Notes for reviewers

Anything non-obvious: trade-offs made, follow-ups deferred, areas you're
unsure about.
