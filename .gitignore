# Rust build output
target/

# JS dependencies — regenerated by `bun install`
node_modules/
**/node_modules/
**/*/node_modules/

# OS and editor noise
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/

*.db
*.db-shm
*.db-wal
# The generated bundle (examples/hello-world/js/app.js) is intentionally
# committed so `cargo build` works on a fresh checkout without requiring
# a prior `bun build` run.
