# Rust build output
target/

# JS dependencies — regenerated by `bun install`
node_modules/
**/node_modules/
**/*/node_modules/

# OS and editor noise
.DS_Store
Thumbs.db
.vscode/settings.json
.idea/

*.db
*.db-shm
*.db-wal
# Compiled JS bundles in example apps are intentionally committed so
# `cargo build` works on a fresh checkout without requiring `bun build`.
# User-created projects exclude their dist/ via their own .gitignore.
