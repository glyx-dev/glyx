[package]
name    = "glyx-tray"
version = "0.1.0"
edition = "2021"

[dependencies]
log       = { workspace = true }
serde     = { workspace = true, features = ["derive"] }
tray-icon = { workspace = true }
muda      = { workspace = true }
