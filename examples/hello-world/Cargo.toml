[package]
name    = "hello-world"
version = "0.1.0"
edition = "2021"

[features]
default = ["dev"]
dev     = ["glyx-core/dev"]

[dependencies]
glyx-core  = { path = "../../crates/glyx-core", default-features = false }
glyx-shell = { path = "../../crates/glyx-shell" }
